#include<stdio.h>
#include<limits.h>
int main()
{
printf("\nThe number of bits in a byte are:",CHAR_BIT);
printf("\nThe values of signed char max, signed char min, and unsigned char max are %d %d %d ", SCHAR_MAX,SCHAR_MIN,UCHAR_MAX);
printf("\nthe values of int max and min are :%d %d",INT_MAX,INT_MIN);
printf("The max value of float is %f",FLOAT_ :MAX);
printf("BY computation");

printf("Size of float is %lu",sizeof(float));
float i=0;
float d;
while(i>0)
{d=i;
i++;
}
printf("Loop Breaking at %f",d);
}





